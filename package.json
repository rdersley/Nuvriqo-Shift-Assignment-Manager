{
  "name": "nuvriqo-shift-assignment-manager",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "echo 'UI Kit resource is bundled by Forge'",
    "lint:domain": "node --check src/domain/shifts.js && node --check src/engine/rules.js && node --check src/engine/assignment.js && node --check src/index.js",
    "check": "npm run lint:domain && npm test"
  },
  "dependencies": {
    "@forge/api": "^5.0.0",
    "@forge/bridge": "^5.0.0",
    "@forge/kvs": "^1.0.0",
    "@forge/react": "^11.0.0",
    "@forge/resolver": "^1.7.0",
    "react": "^18.3.1"
  }
}
